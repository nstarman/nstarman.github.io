node_modules/
dist/
.astro/

# Claude's working notebook for this rebuild — local only, deliberately untracked.
Notes.md

.DS_Store
.env
.env.production

# cv.typ reads this; it is copied in per preset by scripts/build-cv-pdfs.sh
cv/cv.json

# local dev-server config
.claude/

# python bytecode from the scripts in scripts/
__pycache__/

# The compiler wasm and fonts are vendored for the CV builder route only.

# generated from data/ and schema/ by scripts/publish-data.mjs
public/records/

# built by scripts/build-cv-pdfs.sh
public/cv/
public/files/
